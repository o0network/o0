name: www.yaml

on:
  push:
    branches:
      - main
    paths:
      - "www/**"

jobs:
  build:
    name: Build and Deploy WWW
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: 'yarn'
          cache-dependency-path: 'www/yarn.lock'

      - name: Install dependencies
        run: cd www && yarn install

      - name: Build and Deploy
        run: cd www && yarn build && rsync -avz --delete dist/ ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/var/www/o0/www/
